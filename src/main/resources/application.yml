spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:file:~/test
    username: sa
    password:
    driverClassName: org.h2.Driver
    initialization-mode: always

  jpa:
    hibernate:
      ddl-auto: create-drop

  profiles:
    active: dev
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

server:
  port: 8080
